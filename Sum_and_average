#include <stdio.h>
int main() {
  int sum = 0, average;
  int x[5] = {1, 3, 10, 13, 5};

  for (int i = 0; i < 5; ++i) {
    sum += x[i];
  }
  average = sum / 5;

  printf("sum = %d, ", sum);
  printf("average = %d", average);
}
