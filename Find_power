#include <math.h>
#include <stdio.h>

void main() {
  int base, exponent;
  printf("Enter base and exponent: \n");
  scanf("%d%d",&base,&exponent);
  int result;
  result = pow(base, exponent);
  printf("Result = %d", result);
}
